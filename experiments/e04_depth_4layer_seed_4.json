{
  "name": "e04_depth_4layer_seed_4",
  "topology": [
    196,
    10,
    10,
    10,
    10
  ],
  "hidden_activation": "relu",
  "data": {
    "x_train": "data/X_train_small.csv",
    "y_train": "data/Y_train_small.csv",
    "x_test": "data/X_test_small.csv",
    "y_test": "data/Y_test_small.csv"
  },
  "train": {
    "epochs": 1000,
    "batch_size": 128,
    "seed": 4
  },
  "sparsify": {
    "steps": 500,
    "do_adjust": true,
    "omega_samples": 10000,
    "checkpoint_every": 50,
    "omega_seed": 4
  },
  "_experiment": {
    "id": "E4b_seed4",
    "description": "[seed 4] Depth scaling \u2014 deep network with 3 hidden layers [196,10,10,10,10]. Total prunable weights: 196*10 + 10*10 + 10*10 + 10*10 = 2260. Deeper networks have longer gradient chains in the adjust step, potentially causing the compensation to be less effective for early layers (gradient vanishing). The per-layer NZ columns will reveal whether the algorithm concentrates pruning in the later layers where gradients are stronger.",
    "literature": [
      "Dong et al. 2017 (layer-wise OBS): motivates layer-wise decomposition precisely because global second-order methods scale poorly with depth \u2014 this experiment probes where global manifold-based adjust starts to degrade",
      "Molchanov et al. 2019 (importance estimation): finds that cross-layer importance interactions matter \u2014 depth is the key variable"
    ]
  }
}
